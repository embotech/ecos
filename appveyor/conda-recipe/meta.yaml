package:
  name: ecos

requirements:
  build:
    - python
    - numpy
    - setuptools
    - pip

  run:
    - python
    - numpy
    - scipy

test:
    requires:
        - nose
    commands:
        - nosetests --exe ecos
about:
  home: https://github.com/ifa-ethz/ecos
  license: GPLv3
  summary: Embedded cone solver for second-order cone programs