language: c
env:
    - CFLAGS="-fprofile-arcs -ftest-coverage"
compiler:
    - gcc
    - clang
before_install:
    - pip install cpp-coveralls
script:
    - make
    - make test
    - ./runecos
    - ./runecosexp
    - ./ecostester
    - ./ecos_bb_test
after_auccess:
    - coveralls --verbose --dryrun --gcov-options '\-lp'
